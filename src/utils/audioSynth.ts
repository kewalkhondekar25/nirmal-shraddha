// Clean no-op audio utility (Audio/music features disabled per user preference)

export const playTempleBell = () => {
  // No-op
};

export const toggleOmDrone = (_enable: boolean): boolean => {
  return false;
};
